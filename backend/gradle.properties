# Gradle 빌드 설정
#
# ⚠️ JDK 경로(org.gradle.java.installations.paths)를 하드코딩하지 않는다.
#    맥/윈도우 경로가 달라 팀원 빌드가 깨진다. toolchain(자동 탐지/프로비저닝)에 맡긴다.

# 빌드 캐시 · 병렬 실행으로 빌드 속도 향상
org.gradle.caching=true
org.gradle.parallel=true
