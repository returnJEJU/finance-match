<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.financematch.invitation.mapper.InvitationMapper">

    <select id="findActiveInviteCodeByMemberId" resultType="string">
        SELECT ic.code_value
        FROM common_survey cs
            INNER JOIN invitation_code ic
            ON ic.common_survey_id = cs.id
        WHERE cs.member_id = #{memberId}
            AND ic.status = 'ACTIVE'
    </select>

    <select id="lockMemberById" resultType="long">
        SELECT id
        FROM member
        WHERE id = #{memberId}
          AND status = 'ACTIVE'
          AND deleted_at IS NULL
        FOR UPDATE
    </select>

    <select id="existsCoupleByMemberId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM couple
            WHERE inviter_id = #{memberId}
               OR invitee_id = #{memberId}
        )
    </select>

    <select id="existsActiveInvitationByMemberId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1
            FROM common_survey cs
                INNER JOIN invitation_code ic
                ON ic.common_survey_id = cs.id
            WHERE cs.member_id = #{memberId}
                AND ic.status = 'ACTIVE'
        )
    </select>

    <select id="findCommonSurveyByAccessibleMemberId"
            resultType="com.financematch.invitation.dto.CommonSurveyResponse">
        SELECT
            cs.first_goal_type AS goal_type1,
            cs.second_goal_type AS goal_type2,
            cs.target_amount,
            cs.target_period_months,
            cs.loan_purpose,
            cs.has_loan_within_one_month
        FROM common_survey cs
        WHERE cs.member_id = #{memberId}
            OR EXISTS (
                SELECT 1
                FROM couple c
                    INNER JOIN invitation_code ic
                    ON ic.id = c.invitation_code_id
                WHERE c.invitee_id = #{memberId}
                    AND ic.common_survey_id = cs.id
            )
    </select>

    <insert id="insertCommonSurvey"
            parameterType="com.financematch.invitation.domain.CommonSurvey"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO common_survey (
            member_id,
            first_goal_type,
            second_goal_type,
            target_amount,
            target_period_months,
            loan_purpose,
            has_loan_within_one_month
        ) VALUES (
            #{memberId},
            #{firstGoalType},
            #{secondGoalType},
            #{targetAmount},
            #{targetPeriodMonths},
            #{loanPurpose},
            #{hasLoanWithinOneMonth}
        )
    </insert>

    <insert id="insertInvitationCode">
        INSERT IGNORE INTO invitation_code (
            common_survey_id,
            code_value,
            status
        ) VALUES (
            #{commonSurveyId},
            #{codeValue},
            'ACTIVE'
        )
    </insert>

</mapper>