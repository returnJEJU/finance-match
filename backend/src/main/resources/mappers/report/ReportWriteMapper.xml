<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.financematch.report.mapper.ReportWriteMapper">

    <insert id="upsertGoalFeasibilityReason">
        INSERT INTO report (compatibility_result_id, goal_feasibility_reason, expected_asset)
        VALUES (#{compatibilityResultId}, #{reason}, #{expectedAsset})
        ON DUPLICATE KEY UPDATE goal_feasibility_reason = VALUES(goal_feasibility_reason),
                                 expected_asset = VALUES(expected_asset)
    </insert>

    <insert id="upsertAssetStabilityReason">
        INSERT INTO report (compatibility_result_id, asset_stability_reason)
        VALUES (#{compatibilityResultId}, #{reason})
        ON DUPLICATE KEY UPDATE asset_stability_reason = VALUES(asset_stability_reason)
    </insert>

    <insert id="upsertDebtRepaymentReason">
        INSERT INTO report (compatibility_result_id, debt_repayment_reason)
        VALUES (#{compatibilityResultId}, #{reason})
        ON DUPLICATE KEY UPDATE debt_repayment_reason = VALUES(debt_repayment_reason)
    </insert>

    <insert id="upsertFinancialValueReason">
        INSERT INTO report (compatibility_result_id, financial_value_reason)
        VALUES (#{compatibilityResultId}, #{reason})
        ON DUPLICATE KEY UPDATE financial_value_reason = VALUES(financial_value_reason)
    </insert>

    <insert id="upsertTaxStrategyReason">
        INSERT INTO report (compatibility_result_id, tax_strategy_reason)
        VALUES (#{compatibilityResultId}, #{reason})
        ON DUPLICATE KEY UPDATE tax_strategy_reason = VALUES(tax_strategy_reason)
    </insert>
</mapper>
